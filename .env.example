# Turso (libSQL) database. If unset, the app falls back to a local SQLite
# file (./local.db) so development works with zero setup.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Secret used to sign session cookies. Required in production; in
# development an insecure default is used if this is unset.
AUTH_SECRET=
